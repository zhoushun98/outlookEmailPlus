# 文档审查报告：Outlook邮件管理工具-前后端拆分与模块化（00002）

## 📄 文档信息

- 主审文档：`docs/TDD/Outlook邮件管理工具-前后端拆分与模块化TDD.md`
- 文档类型：TDD
- 文档编号：未解析到 5 位数字编号（建议统一分配编号并落到文件名：如 `TDD-00002-前后端拆分与模块化.md`）
- 文档标题：Outlook 邮件管理工具｜前后端拆分与模块化 TDD（技术设计细节）
- 存放路径：`docs/TDD/Outlook邮件管理工具-前后端拆分与模块化TDD.md`
- 关联前置文档（同主题，因缺少编号以标题关联）：
  - `docs/PRD/Outlook邮件管理工具-前后端拆分与模块化PRD.md`
  - `docs/FD/Outlook邮件管理工具-前后端拆分与模块化FD.md`
  - `docs/TODO/Outlook邮件管理工具-前后端拆分与模块化TODO.md`

## 📚 规范与架构参考文件读取结果

- 文档规范：
  - 未找到 `Rules/开发文档规范.md`
  - 未找到 `Rules/项目文档规范.md`
- 项目架构/产品 Steering：
  - 未找到 `.kiro/steering/structure.md`
  - 未找到 `.kiro/steering/tech.md`
  - 未找到 `.kiro/steering/product.md`

> 影响：部分“命名/必备章节/项目架构符合性”无法按项目权威规范判定。本报告按你提供的审查维度清单做最佳努力评估，并基于本仓库（Python/Flask）现实情况对“集成点检查”做等价映射（见下文“功能集成完整性”）。

---

## 📊 质量评分

- 格式规范性：5/10
- 内容完整性：7/10
- 开发可行性：7/10
- 架构符合性：7/10
- 功能集成性：6/10
- 整体评分：6.4/10

---

## ✅ 优点

- **拆分目标清晰且可回滚**：明确“行为兼容 + 渐进式拆分 + 每阶段可验收/可回滚”，适合在现有稳定版本上做结构重构（`docs/TDD/Outlook邮件管理工具-前后端拆分与模块化TDD.md` 2/6）。
- **分层边界与依赖方向写得明确**：给出了 `routes -> services -> repositories -> db` 的依赖约束，并强调避免循环依赖（3.1、8）。
- **关键风险点识别到位**：import-time 副作用（调度器线程）、契约漂移、前端全局变量依赖等风险都有对应缓解策略（4.2、4.9、8）。
- **与 PRD/FD 一致**：PRD/FD 的“对用户无感、不引入新复杂度、保持接口不变”的核心约束，在 TDD 中被复述并落到迁移阶段与测试门禁（PRD 2/3/5，FD 2，TDD 2/6/7）。

---

## ⚠️ 需要改进项

### 1) 格式规范（命名/编号/类型）

- 当前文档命名未体现“类型-编号-描述”的规则（无法自动按编号关联前置文档）。
- 你提供的文档类型集合里未包含 `TODO`，但仓库中大量使用 TODO 文档；建议在“项目文档规范”中明确是否允许 TODO 作为文档类型。

### 2) 内容完整性（TDD 维度）

> 当前 TDD 已是“技术设计细节”，明显比旧的“技术交付清单型 TDD”更可落地；但仍建议补齐两类“可直接验收/可直接开发”的信息。

- **缺少“接口契约抽样清单”**：虽强调 URL/响应不变，但未列出需保持不变的关键接口清单与最小字段契约（建议附录列出 10~20 个关键端点与关键字段）。
- **缺少“迁移后的模块间契约”**：例如 repository 返回的数据形态、service 的错误聚合结构是否必须与现有一致，建议用 1~2 个典型链路给出示例（例如“删除邮件三路回退错误聚合”）。

### 3) 前端拆分的“切割策略”可再具体

- 当前给了 `api/ui/features/state` 的理想结构，但未说明迁移时如何处理：
  - 现有全局函数/事件绑定的过渡方案（例如先 `main.js` 保持旧函数名，再逐步导出/内聚）
  - 静态资源缓存/版本策略（避免浏览器缓存导致上线后脚本不更新）

---

## ❌ 严重问题

### S-001：缺少项目级“文档规范/架构 Steering”，导致审查标准难以固化

- 问题描述：未找到 `Rules/*` 与 `.kiro/steering/*`，无法严格按项目规范判定“必备章节/命名/目录结构”。
- 影响：后续文档增多时一致性会下降，审查结论难以复用。
- 建议：补一份最小可用的 `Rules/项目文档规范.md`（至少定义：允许的文档类型、命名规则、存放路径、各类型必备章节）。

---

## 🔗 前置文档一致性

- PRD → FD：
  - 一致性：✅（FD 的交付项覆盖 PRD 目标：后端模块化、入口兼容、前端静态资源拆分、测试与文档交付）。
  - 风险：⚠️（FD 对“接口契约不变”的验收口径仍偏抽象，可在 FD/TODO 中增加“抽样验收接口清单”以便执行）
- FD → TDD：
  - 一致性：✅（TDD 细化了 FD 的拆分交付项：目录结构、装配策略、依赖方向、分阶段迁移与测试门禁）
  - 缺口：⚠️（TDD 尚未给出“最小契约清单”和“典型链路示例”，导致实现时仍可能出现局部行为漂移）
- TDD → TODO：
  - 一致性：✅（TODO 的阶段拆解与 TDD 6 章一致）

---

## 🔗 功能集成完整性检查（重点，适用 TDD）

> 说明：你提供的集成点清单以 Android（导航/DI/Room/Hilt）为例。本仓库为 Python/Flask Web 项目，这里采用等价映射：
>
> - “导航集成” ≈ Blueprint/路由注册 + 前端入口脚本可达
> - “DI 模块集成” ≈ app 装配(create_app)/依赖装配/初始化顺序
> - “数据库集成” ≈ init_db/迁移/连接生命周期
> - “入口点集成” ≈ 页面/按钮/路由是否可访问
> - “调用链完整” ≈ routes → services → repositories → db

### 功能组件清单（从 TDD 抽取）

| 组件类型 | 组件名称（建议） | 集成状态 |
|---------|------------------|---------|
| App 装配 | `outlook_web.app:create_app()` | ⚠️ 设计已明确，尚需落地并补 smoke test |
| 路由注册 | `outlook_web.routes.*`（Blueprint） | ⚠️ 设计已明确，尚需落地与契约抽样测试 |
| 服务层 | `outlook_web.services.*` | ⚠️ 设计已明确，尚需迁移与单测 |
| 仓储层 | `outlook_web.repositories.*` | ⚠️ 设计已明确，尚需迁移与单测 |
| DB/迁移 | `outlook_web.db:init_db()` | ⚠️ 设计已明确，需验证与现有迁移一致 |
| 调度器 | `outlook_web.scheduler.*` | ⚠️ 设计已明确，重点验证 import-time 副作用 |
| 前端入口 | `/static/js/main.js` | ⚠️ 设计已明确，需验证模板引用与缓存策略 |

### 集成点验证（以“文档可验收”为准）

| 集成点 | 文档中是否明确 | 状态 | 说明 |
|--------|----------------|------|------|
| 路由/导航注册 | ✅ | ✅ | 明确 Blueprint 注册方式与 URL 不变策略（4.8） |
| 装配/初始化顺序 | ✅ | ✅ | `init_db()` → scheduler 开关启动（4.9） |
| 调用链完整性 | ✅ | ✅ | 分层依赖方向写明（3.1） |
| 静态资源入口 | ✅ | ⚠️ | 明确 `<script type="module"...>`，但缺少缓存/版本策略（5.1） |
| 测试门禁 | ✅ | ✅ | 每阶段单测 + 抽样接口验收（6/7） |

### ❌ 集成缺失问题（文档层面）

#### INT-001：缺少“关键接口契约抽样清单”

- 问题描述：文档强调“接口不变”，但未列出要抽样验收的关键端点与关键字段。
- 影响：实施阶段很容易出现“字段名/错误码/细节结构”不小心漂移，回归成本上升。
- 建议：在 TDD 或 TODO 的 P0-0 增加一个“契约清单附录”（最小集合即可）。

#### INT-002：缺少“静态资源缓存策略”

- 问题描述：从内联脚本迁移到静态资源后，会引入浏览器缓存带来的“发布后脚本不更新”问题。
- 建议：在 TDD 5.1 增加明确策略（例如 querystring 版本号、文件名 hash、或在响应头层面控制）。

---

## 📋 改进建议（按优先级）

### P0（建议立即补齐）

1. 在仓库补齐最小文档规范：`Rules/项目文档规范.md`（文档类型集合、命名/目录、必备章节模板）。
2. 为“前后端拆分与模块化”主题分配编号并统一文件名（PRD/FD/TDD/TODO/CR），以便自动关联与审查流程自动化。
3. 在 TDD 增加“关键接口契约抽样清单”（10~20 个端点 + 关键字段/错误结构要点）。
4. 在 TDD 增加“静态资源缓存/版本策略”说明，避免上线后缓存问题。

### P1（实施过程中逐步补齐）

5. 增加 1~2 条“典型链路示例”（例如：删除邮件三路回退错误聚合；刷新 SSE 事件序列），作为迁移时的行为锚点。
6. 在 TODO 的 P0-0 增加 smoke test 的明确检查点与测试文件路径（便于落地时直接实现）。

